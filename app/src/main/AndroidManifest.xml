<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wellingtonmartins.controlabastecimento">

    <application
        android:allowBackup="true"
        android:icon="@drawable/abastecimento1"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.AbastecimentoActivity"

            >

        </activity>

        <activity
            android:name=".activity.CadVeiculoActivity"

            />
        <activity
            android:name=".activity.ListarVeiculoActivity"

            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
